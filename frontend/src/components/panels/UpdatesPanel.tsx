/**
 * Updates panel — static placeholder (docs Tab 6).
 */
export default function UpdatesPanel() {
	return (
		<section aria-labelledby="updates-heading">
			<h2 id="updates-heading">Updates</h2>
			<p>No updates yet.</p>
		</section>
	);
}
